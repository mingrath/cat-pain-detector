# Data Directory

This folder is intentionally gitignored for large/private datasets.

Current local data:

- `raw/catflw/` — CatFLW downloaded from Kaggle (`georgemartvel/catflw`), CC BY-NC 4.0, for landmark validation only.
- FGS-labeled pain datasets are pending author request; do not claim model accuracy until labels are available or a clearly documented validation set is created.
